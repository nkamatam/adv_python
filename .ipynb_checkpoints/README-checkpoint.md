*Kishan's Commands*
```
rm -rf * 
git clone https://github.com/nkamatam/adv_python.git

#Cloning 
cd  adv_python/
#create the file, File -> New -> Nootbook ->rename the notebook
git config --global user.email "kishan.p.mashru@gmail.com"
git config --global user.name "kishan-mashru" 

# To check_in your changes
git add .

git commit -m "checking in the file"
git push
#user-name :
#pwd :
```

*KNK's Commands*
```
rm -rf * 
git clone https://github.com/nkamatam/adv_python.git

#Cloning 
cd  adv_python/
#create the file, File -> New -> Nootbook ->rename the notebook
git config --global user.email "nkamatam@gmail.com"
git config --global user.name "KNK" 

# To check_in your changes
git add .
git commit -m "checking in the file"
git pull
git push

# Repeat the pull and push multiple times if there are some conflicts
#user-name :
#pwd :
```

*Thriveni's Commands*
```

rm -rf * 
git clone https://github.com/nkamatam/adv_python.git

#Cloning 
cd  adv_python/
#create the file, File -> New -> Nootbook ->rename the notebook
git config --global user.email "thriveniravi3@gmail.com"
git config --global user.name "thriveni-git" 

# To check_in your changes
git add .
git commit -m "checking in the file by thriveni"
git pull
git push

# Repeat the pull and push multiple times if there are some conflicts
#user-name :
#pwd :
```